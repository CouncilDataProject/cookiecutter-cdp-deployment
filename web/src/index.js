import React from 'react';
import ReactDOM from 'react-dom';
// import {App} from '@councildataproject/cdp-frontend';

// Temporary App component until the CDP App component is 
// finished and published
function App() {
  return <h1>CDP App Instance</h1>
}

ReactDOM.render(<App />, document.getElementById('root'));
