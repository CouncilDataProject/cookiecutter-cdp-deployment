default_context:
    municipality: "{{ cookiecutter.municipality }}"
    iana_timezone: "{{ cookiecutter.iana_timezone }}"
    governing_body_type: "{{ cookiecutter.governing_body_type }}"
    municipality_slug: "{{ cookiecutter.municipality_slug }}"
    python_municipality_slug: "{{ cookiecutter.python_municipality_slug }}"
    infrastructure_slug: "{{ cookiecutter.infrastructure_slug }}"
    maintainer_or_org_full_name: "{{ cookiecutter.maintainer_or_org_full_name }}"
    hosting_github_username_or_org: "{{ cookiecutter.hosting_github_username_or_org }}"
    hosting_github_repo_name: "{{ cookiecutter.hosting_github_repo_name }}"
    hosting_github_url: "{{ cookiecutter.hosting_github_url }}"
    hosting_web_app_address: "{{ cookiecutter.hosting_web_app_address }}"
    firestore_region: "{{ cookiecutter.firestore_region }}"
    event_gather_timedelta_lookback_days: {{ cookiecutter.event_gather_timedelta_lookback_days }}
    event_gather_cron: "{{ cookiecutter.event_gather_cron }}"
    enable_clipping: "{{ cookiecutter.enable_clipping }}"
    speech_to_text_model_version: "{{ cookiecutter.speech_to_text_model_version }}"
    event_gather_runner_timeout_minutes: "{{ cookiecutter.event_gather_runner_timeout_minutes }}"
    event_gather_runner_max_attempts: "{{ cookiecutter.event_gather_runner_max_attempts }}"
    event_gather_runner_retry_wait_seconds: "{{ cookiecutter.event_gather_runner_retry_wait_seconds }}"
